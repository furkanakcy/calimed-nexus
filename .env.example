# Backend .env variables (for server/.env)
DATABASE_URL="postgresql://calimed:secure_password@calimed-db.internal:5432/calimed_nexus"
JWT_SECRET="super_secret_key_change_in_production"
NODE_ENV="production"

# Frontend .env variables (for root .env)
VITE_API_URL="https://calimed-backend.fly.dev"

# Fly.io deployment commands:
# Backend secrets:
# flyctl secrets set DATABASE_URL="postgresql://calimed:secure_password@calimed-db.internal:5432/calimed_nexus"
# flyctl secrets set JWT_SECRET="super_secret_key_change_in_production"

# Frontend build arg:
# flyctl deploy --build-arg VITE_API_URL="https://calimed-backend.fly.dev"
